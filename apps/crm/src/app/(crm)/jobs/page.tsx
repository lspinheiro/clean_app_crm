import { PlaceholderPage } from "@/components/placeholder-page";

export default function JobsPage() {
  return <PlaceholderPage title="Jobs" description="Manual dispatch arrives with the cleaner loop in M3." />;
}
