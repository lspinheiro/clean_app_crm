{
  "name": "clean-app-crm",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "crm": "pnpm --filter crm",
    "cleaner": "pnpm --filter cleaner",
    "db:start": "pnpm --filter @clean-app/db db:start",
    "db:stop": "pnpm --filter @clean-app/db db:stop",
    "db:reset": "pnpm --filter @clean-app/db db:reset",
    "db:test": "pnpm --filter @clean-app/db db:test",
    "test": "pnpm --filter \"./apps/*\" run test:run",
    "test:e2e": "pnpm --workspace-concurrency=1 --filter \"./apps/*\" run test:e2e",
    "lint": "pnpm --filter \"./apps/*\" run lint",
    "typecheck": "pnpm --filter \"./apps/*\" run typecheck",
    "build": "pnpm --filter \"./apps/*\" run build",
    "test:dev-setup": "node --test scripts/run-local-dev.test.mjs",
    "test:vocabulary": "node scripts/check-vocabulary.mjs",
    "check": "pnpm test:vocabulary && pnpm test:dev-setup && pnpm test && pnpm db:test && pnpm lint && pnpm typecheck && pnpm build"
  }
}
